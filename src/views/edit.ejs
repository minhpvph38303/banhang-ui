<!DOCTYPE html>
<html>
  <head>
    <title>Sửa</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </head>

  <body class="p-4">
    <div class="container mt-3">
      <h2 style="text-align: center">Sửa khách hàng</h2>

      <form action="/edit/<%= cat.id %>" method="POST">
        <div class="mb-3 mt-3">
          <label>Tên </label>
          <input
            type="text"
            name="name"
            class="form-control"
            value="<%= cat.name %>"
            required
          />
        </div>
        <div class="mb-3 mt-3">
          <label>SDT </label>
          <input
            type="text"
            name="sdt"
            class="form-control"
            value="<%= cat.sdt %>"
            required
          />
        </div>
        <div class="mb-3 mt-3">
          <label for="">Địa chỉ</label>
          <input
            type="text"
            name="diachi"
            class="form-control"
            value="<%= cat.diachi %>"
            required
          />
        </div>
        <button class="btn btn-primary mt-3">Submit</button>
      </form>
    </div>
  </body>
</html>
